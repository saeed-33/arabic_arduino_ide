# Technical Requirements

## Platform

- Target operating system: Windows only.
- Application framework: Flutter desktop.
- Initial supported Flutter target: `windows`.

## Language Files

- Arabic Arduino source files use the `.arabic` extension.
- The IDE may still open `.ino` and `.txt` files for interoperability/testing.

## Local Toolchain

- Flutter: `3.41.6`
- Flutter channel: `stable`
- Flutter framework revision: `db50e20168`
- Flutter engine revision: `425cfb54d0`
- Dart: `3.11.4`
- DevTools: `2.54.2`

## Dart SDK Constraint

From `IDE/pubspec.yaml`:

```yaml
environment:
  sdk: ^3.11.4
```

From `IDE/pubspec.lock`:

```text
dart: >=3.11.4 <4.0.0
flutter: >=3.38.0
```

## Direct Runtime Libraries

| Library | Version | Purpose |
| --- | --- | --- |
| `flutter` | SDK | Flutter UI framework. |
| `cupertino_icons` | `1.0.9` | Icon font dependency generated by Flutter scaffold. |
| `file_selector` | `1.1.0` | Native Windows open/save file dialogs. |

## Direct Development Libraries

| Library | Version | Purpose |
| --- | --- | --- |
| `flutter_test` | SDK | Widget and unit testing. |
| `flutter_lints` | `6.0.0` | Recommended Flutter lint rules. |

## Relevant Transitive Libraries

| Library | Version | Purpose |
| --- | --- | --- |
| `file_selector_windows` | `0.9.3+5` | Windows implementation for `file_selector`. |
| `file_selector_platform_interface` | `2.7.0` | Platform interface for file selector plugins. |
| `cross_file` | `0.3.5+2` | Cross-platform file abstraction used by file selector. |
| `http` | `1.6.0` | Transitive dependency currently brought by file selector package graph. |

## Provided Compiler Source

Compiler source snapshot:

```text
compiler/ArduinoArabicCompiler/
```

Files imported from the provided archive:

- `ArArduinoLexer.g4`
- `ArArduinoParser.g4`
- `requirements.txt`
- `test.txt`
- `parse tree.png`

Compiler source type:

- ANTLR grammar files.
- Python runtime requirements.

Compiler Python requirements from `compiler/ArduinoArabicCompiler/requirements.txt`:

| Library | Version |
| --- | --- |
| `antlr4-python3-runtime` | `4.13.2` |
| `llvmlite` | `0.47.0` |

Developer Mode executes the compiler diagnostics runner through a Dart adapter contract.

Runner:

```text
compiler/ArduinoArabicCompiler/run_diagnostics.py
```

Local setup:

```powershell
cd compiler/ArduinoArabicCompiler
.\setup.ps1
```

Current compiler output:

- tokens
- parse tree
- raw syntax diagnostics
- build stages
- internal logs

Current compiler gaps:

- AST output is not emitted by the provided compiler snapshot yet.
- Arduino/C++ generated code is not emitted by the provided compiler snapshot yet.

## Native Plugin Rule

When adding a Flutter desktop plugin, do a full stop and rebuild of the Windows app.

Hot reload and hot restart are not enough to register new native plugins. If the app was already running before the plugin was added, Windows file dialogs can throw:

```text
MissingPluginException(No implementation found for method openFile/getSavePath)
```

Recovery command sequence:

```powershell
cd IDE
flutter clean
flutter pub get
flutter run -d windows
```

## Current Architecture Rule

Feature code should stay separated by responsibility:

- UI widgets coordinate user interaction and rendering.
- Application services perform platform and file operations.
- Controllers own feature state and orchestration.
- Domain models represent structured data without UI dependencies.
- Shared behavior should move into `lib/shared/` only when it is used by more than one feature.

Avoid large "god classes" that mix UI layout, platform calls, parsing, compiler behavior, logging, and device control.

Developer diagnostics should remain isolated from learner-facing modes:

- Developer Mode can show raw compiler/parser details.
- Pro Mode should eventually show learner-appropriate messages by default.
- Kids Mode should avoid raw diagnostics unless explicitly requested by a mentor/developer workflow.

Compiler integration uses an adapter boundary:

- Compiler inputs and outputs should be translated into Dart domain models.
- Flutter widgets should not import or invoke compiler internals directly.
- The current integration runs the compiler diagnostics script as a Python subprocess.

## Current Feature State Storage

Settings are in-memory only in Step 7.

No persistent storage package is installed yet. A later step should choose a Windows-friendly local storage strategy before settings are expected to survive app restarts.
